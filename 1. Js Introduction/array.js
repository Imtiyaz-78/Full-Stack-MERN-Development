  
   
  // Arrays

  /*
    let arr = [1, 2, 3, 4] // initilization of array
    console.log(arr)

    let arr1 = [1, 'Hello', 3, 'true'] // In The javaScript you can put any kind of data in array 
    console.log(arr1)
 */


  
    // Array Methods 1. Replace

  let cars = ["BMW", "Jaguar", "Mercedes"]
   console .log(cars)
 /*
     // Array Method-2: Repleace an elemnet in an arry
     cars[2]= "Odey"
     console .log(cars)

    // Array Method-3: Add a new element to an array
    cars[3] = 'Audi'
    console.log(cars)

    // Array Method-4: Calculate the Length of Array
    let length = cars.length
    console.log(length) 

    // Array Method-4: Push and pop
    let poppedCar = cars.pop() // Push method add the element at the ned of array
    console.log(cars)

    cars.push('Bolero') // Pop method remove the element at the end of array 
    console.log(cars)  

  // Shift Method: it removes an element from the starting of the array
    
   let shiftCar = cars.shift() 
     console.log(shiftCar)
     console.log(cars) 
    
*/

  // unshift Method: it adds an element at the Start of the array
  cars.unshift('Alto')
  console.log(cars)







